<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Product</title>
    <link rel="stylesheet" href="/navbar.css">
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        .formcontainer {
            padding: 120px 20px;
            max-width: 800px;
            margin: 0 auto;
            background-color: #f9f9f9;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .image{
             width: 50%;
    height: auto;
    display: block; 
    margin-left: auto;
    margin-right: auto; 

        }

        .formcontainer h1 {
            text-align: center;
            margin-bottom: 40px;
            color: #05944f;
        }

        .form-tags {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-between;
        }

        .form-item {
            width: 47%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ccc;
            border-radius: 4px;
            font-size: 16px;
        }

        .form-item::placeholder {
            color: #aaa;
        }

        .form-item:focus {
            outline: none;
            border-color: #05944f;
        }

        .form-tags .form-item.full-width {
            width: 100%;
        }
        .select-item{
            width: 49.6%;
        }
        .placeholder{
            color: #aaa;
        }
       

        .add-button {
            display: block;
            width: 100%;
            padding: 15px;
            background-color: #05944f;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

.confirmation-message {
    color: green;
    margin-top: 10px;
    padding: 10px;
    text-align: center;
    background-color: #e0ffe0;
    border: 1px solid #05944f;
    border-radius: 4px;
    display: none;

}

        .add-button:hover {
            background-color: #03793e;
        }
        
        @media (max-width: 768px) {
            .form-item {
                width: 100%;
            }
        }
        
    </style>
     <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
   
</head>
<body>
    {{> navbar }}

    <div class="formcontainer">
        
          <img class="image" src="{{product.image}}"> 
        <form id="productForm" >
           
            <div class="form-tags">
                <input class="form-item" type="text" name="name"  value="{{product.name}}"> 
                <input class="form-item" type="text" name="price"   value="{{product.price}}"> 
                <input class="form-item" type="text" name="seller"   value="{{product.seller}}"> 
                 <select class="form-item select-item" name="category" >
           <option value="" disabled selected  class="placeholder">Select Category</option>
            <option value="vegetable" >Vegetable Seeds</option>
            <option value="fruit">Fruit Seeds</option>
            <option value="flower">Flower Seeds</option>
            <option value="herb">Herb Seeds</option>
            <option value="grain">Grain & Cereal Seeds</option>
            <option value="sprouting">Sprouting Seeds</option> 
             <option value="others">Others</option>
          
            </select>
                <input class="form-item full-width" type="text" name="description" value="{{product.description}}">  
                <input class="form-item full-width" type="text" name="image"  value="{{product.image}}"> 
                <input type="hidden" name="id" value="{{product.id}}" >
            </div>
            <button class="add-button" type="submit">EDIT</button>
        </form>
         <div id="confirmationMessage" class="confirmation-message"></div>
    </div>
     <script src="/updatescript.js"></script>
</body>
</html>
